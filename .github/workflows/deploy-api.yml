name: Build & Deploy Motoscope API (GHCR)

on:
  push:
    branches: [ "main" ]
    # TEMP: remove paths filter so we always trigger while debugging
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Workflow syntax OK"

                          
